
disp('Enter Co-Efficient Matrix:');

r=input('Enter Unknown Var Nos : ');
c=r;
%A=ScanMatrix(r,c);
A=zeros(r,c);

disp('Enter Right Hand Side Matrix:');

%b=ScanMatrix(r,1);


disp('Solution Matrix : ');

%disp(LU_Decomposition(A,b));
